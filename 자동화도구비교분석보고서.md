# 자동화 도구 비교 구현 보고서
## Make vs n8n

> **작성 목적:** 동일한 자동화 워크플로우를 Make와 n8n 두 도구로 구현하고, 각 도구의 특성을 비교 분석한다.

---

## 1. 워크플로우 개요

**구현 워크플로우:**
> "RSS 피드 수집 → AI 요약 생성 → Notion 데이터베이스 자동 저장"

| 항목 | 내용 |
|------|------|
| RSS 소스 | TechCrunch (https://feeds.feedburner.com/TechCrunch/) |
| AI 처리 | 기사 제목 기반 3줄 요약 생성 |
| 저장 위치 | Notion 데이터베이스 (Name / Summary / URL / Published / Source) |
| 목적 | 최신 뉴스를 자동으로 수집·요약하여 Notion에 정리 |

---

## 2. Make 구현

### 워크플로우 구성

```
[RSS Feed] → [Router] → (true)  [OpenAI GPT] → [Notion - Create Page]
                      → (false) [Notion - Create Page (미분류)]
```

### 단계별 설정

| 단계 | 모듈 | 설정 내용 |
|------|------|-----------|
| Trigger | RSS Feed – Watch RSS Feed Items | RSS URL 입력, 최대 5개 기사 수집 |
| Flow | Router | 조건 분기 처리 (AI 요약 대상 / 미분류 분리) |
| Action 1 | OpenAI – Create a Completion | 기사 제목을 프롬프트로 전달, 3줄 요약 생성 |
| Action 2 | Notion – Create a Page | DB 연결 후 5개 필드 매핑 (AI 요약본 저장) |
| Action 3 | Notion – Create a Page | DB 연결 후 5개 필드 매핑 (미분류 저장) |

### 주요 설정 특이사항

- **스케줄 실행:** 15분 간격으로 RSS 피드를 주기적으로 확인
- **배치 처리:** 한 번 실행 시 최대 5개 기사를 한꺼번에 처리
- **Router 모듈:** 조건에 따라 두 경로로 분기, 각각 다른 Notion DB에 저장
- **OpenAI 연동:** API Key 직접 입력 필요 (토큰 비용 발생)
- **Notion 연결:** Integration Token 방식으로 DB 연결

---

## 3. n8n 구현

### 워크플로우 구성

```
[RSS Feed Trigger] → [Limit] → [IF] → (true)  [OpenAI - Message a Model] → [Notion - Create Page]
                                     → (false) [Notion - Create Page1]
```

### 단계별 설정

| 단계 | 노드 | 설정 내용 |
|------|------|-----------|
| Trigger | RSS Feed Trigger | RSS URL 입력, Every Minute 폴링 |
| Flow 1 | Limit | 최대 5개 기사로 제한 |
| Flow 2 | IF | 조건 분기 (AI 요약 대상 / 미분류 분리) |
| Action 1 | OpenAI – Message a Model | 기사 제목 기반 3줄 요약 생성 |
| Action 2 | Notion – Create a database page | AI 요약본 Notion DB 저장 |
| Action 3 | Notion – Create a database page1 | 미분류 기사 Notion DB 저장 |

### 주요 설정 특이사항

- **폴링 방식:** Every Minute 설정으로 주기적으로 RSS 확인
- **Limit 노드:** RSS Trigger와 IF 사이에 별도 노드로 개수 제한
- **IF 노드:** true/false 두 갈래로 분기, 각각 다른 Notion 페이지에 저장
- **OpenAI 연동:** API Key 직접 입력 필요 (토큰 비용 발생)
- **Self-hosted:** 로컬 또는 서버에 직접 설치하여 운영
- **Notion 연결:** API Key 방식으로 DB 연결

---

## 4. 비교 분석표

| 비교 항목 | Make | n8n |
|-----------|------|-----|
| **① UI/UX** | 시각적 캔버스 기반, 모듈 간 연결선으로 흐름 파악 용이 | 노드 기반 캔버스, 개발자 친화적 인터페이스 |
| **② 설정 난이도** | 초기 학습 곡선 있음, 모듈·Operations 개념 이해 필요 | 개발 지식 있으면 유리, 표현식 문법 별도 학습 필요 |
| **③ AI 연동 방식** | OpenAI 전용 모듈 제공, API Key 입력으로 간편 연동 | OpenAI 노드 제공, API Key 입력 방식 동일 |
| **④ 실행 방식** | 스케줄 기반 배치 처리 (한 번에 여러 개) | 폴링 또는 Webhook 기반, 노드 단위 개별 처리 |
| **⑤ 무료 플랜** | 1,000 Operations/월, 시나리오 2개 (클라우드) | Self-hosted 시 **완전 무료**, 클라우드는 유료 |
| **⑥ 실행 로그** | 실행 히스토리에서 단계별 입출력 데이터 상세 확인 | 각 노드 클릭 시 입출력 데이터 실시간 확인 가능 |
| **⑦ 연동 서비스** | 1,000개 이상 앱 지원, HTTP 모듈로 커스텀 API 가능 | 400개 이상 노드 제공, HTTP Request로 커스텀 API 가능 |
| **⑧ 조건 분기** | Router 모듈로 다중 분기 처리 가능 | IF 노드로 true/false 분기, Switch 노드로 다중 분기 |
| **⑨ 에러 처리** | 에러 핸들러 모듈 별도 제공, 세밀한 제어 가능 | Error Trigger 노드 제공, try/catch 방식 처리 가능 |
| **⑩ 가격 정책** | 작업 단위(Operations) 과금, 월정액 구독 | Self-hosted 무료, 클라우드 유료 (오픈소스) |
| **⑪ 설치 방식** | 클라우드 SaaS (별도 설치 불필요) | Self-hosted (Docker/npm 설치) 또는 클라우드 |
| **⑫ 커스터마이징** | 제공 모듈 범위 내에서 설정 | JavaScript 코드 직접 작성 가능, 자유도 높음 |

---

## 5. 각 도구의 장단점

### ✅ Make

**장점**

- 노드 기반 시각화로 복잡한 워크플로우도 한눈에 파악 가능
- Router, Iterator, Aggregator 등 고급 모듈 기본 제공
- 클라우드 기반으로 별도 서버 설치 없이 바로 사용 가능
- HTTP 모듈로 API 직접 호출 가능 → 확장성 높음
- 에러 핸들링, 반복 처리 등 세밀한 제어 가능
- 비개발자도 시각적으로 워크플로우 이해 가능

**단점**

- 처음 사용 시 학습 곡선 존재 (Operations 개념 이해 필요)
- AI 연동 시 OpenAI API Key 및 비용 별도 필요
- 무료 플랜 Operations 소진 시 워크플로우 중단
- 데이터가 Make 클라우드 서버를 경유 (보안 민감 데이터 주의)

---

### ✅ n8n

**장점**

- **Self-hosted 시 완전 무료** → 비용 부담 없이 무제한 사용
- 오픈소스로 소스코드 공개, 커스터마이징 자유도 높음
- JavaScript 코드 노드로 복잡한 로직 직접 구현 가능
- 데이터가 자체 서버에서 처리 → **보안·개인정보 보호에 유리**
- 노드별 실시간 입출력 확인으로 디버깅 편리
- IF / Switch 노드로 다양한 조건 분기 처리 가능

**단점**

- Self-hosted 운영 시 서버 관리 지식 필요 (Docker 등)
- 표현식 문법(`{{ $json.title }}`) 별도 학습 필요
- 공식 지원 노드 수가 Make보다 적음
- 클라우드 버전은 유료, 무료 플랜 없음
- 초보자에게 진입 장벽이 상대적으로 높음

---

## 6. 상황별 도구 추천

| 상황 | 추천 도구 | 이유 |
|------|-----------|------|
| 자동화 입문자, 빠른 구축 필요 | **Make** | 클라우드 기반, 시각적 UI로 빠른 시작 가능 |
| 비용 없이 무제한 자동화 필요 | **n8n** | Self-hosted 시 완전 무료 |
| 복잡한 조건 분기, 반복 처리 필요 | **Make** | Router, Iterator 등 고급 모듈 기본 제공 |
| 보안·개인정보가 중요한 환경 | **n8n** | 자체 서버 운영으로 데이터 외부 유출 없음 |
| 커스텀 코드 로직 구현 필요 | **n8n** | JavaScript 코드 노드 직접 작성 가능 |
| 서버 관리 없이 SaaS로 운영 | **Make** | 클라우드 기반, 인프라 관리 불필요 |
| 개발자가 팀 내 자동화 구축 | **n8n** | 오픈소스, 코드 기반 커스터마이징 강점 |
| 다양한 앱 커넥터 필요 | **Make** | 1,000개 이상 앱 지원, 커넥터 수 우세 |

---

## 7. 결론 및 의견

이번 프로젝트에서 **"RSS → AI 요약 → Notion 저장"** 이라는 동일한 워크플로우를 Make와 n8n으로 각각 구현해보았다.

두 도구 모두 목표한 자동화를 성공적으로 구현할 수 있었으나, **운영 방식과 사용 철학이 근본적으로 다르다는 점**이 가장 큰 차이였다.

- **Make**는 클라우드 기반 SaaS로, 설치 없이 바로 사용할 수 있고 시각적 워크플로우 설계가 강점이다. 비개발자도 복잡한 자동화를 구현할 수 있는 도구다.
- **n8n**은 오픈소스 Self-hosted 도구로, 비용 없이 무제한 사용이 가능하고 코드 기반 커스터마이징이 강점이다. 개발 지식이 있는 사용자에게 특히 유리하다.

> **최종 추천:**
> - 빠르게 자동화를 시작하고 싶은 비개발자라면 → **Make**
> - 비용 절감과 데이터 보안이 중요한 개발자·팀이라면 → **n8n**

두 도구를 모두 경험해본 결과, **Make는 편의성, n8n은 자유도와 비용 효율**에서 각각 뚜렷한 강점을 가지고 있으며, 사용 목적과 환경에 따라 선택하는 것이 중요하다.
